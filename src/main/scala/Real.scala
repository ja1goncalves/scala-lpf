class Real(denominator: Int, numerator: Int)
{
//  def + (num: Real): Real = {
//    if(num.denominator == this.denominator) new Real(num.numerator + this.numerator, this.denominator)
//    else{
//      val new_denominator = num.denominator*this.denominator
//      val numeration1 = (new_denominator/this.denominator)*this.numerator
//      val numaration2 = (new_denominator/num.denominator)*num.numerator
//      new Real(new_denominator, numeration1+numaration2)
//    }
//  }
//
//  def - (num: Real): Real = {
//    if(num.denominator == this.denominator) new Real(num.numerator - this.numerator, this.denominator)
//    else{
//      val new_denominator = num.denominator*this.denominator
//      val numeration1 = (new_denominator/this.denominator)*this.numerator
//      val numaration2 = (new_denominator/num.denominator)*num.numerator
//      new Real(new_denominator, numeration1-numaration2)
//    }
//  }
//
//  def * (num: Real): Real = new Real(this.denominator*num.denominator, this.numerator*num.numerator)
//  def / (num: Real): Real = new Real(this.denominator*num.numerator, this.numerator*num.denominator)
}