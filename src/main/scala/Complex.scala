class Complex(real: Int, imaginary: Int) {
//  def +(number: Complex): Complex = new Complex(number.real + this.real, number.imaginary + this.imaginary)
//
//  def -(number: Complex): Complex = new Complex(number.real - this.real, number.imaginary - this.imaginary)
}